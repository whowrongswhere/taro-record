<block>
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <textarea style="{{anonymousState__temp3}}" placeholder-style="{{placeholderStyle}}"
        placeholder-class="{{anonymousState__temp4}}" cursor-spacing="{{cursorSpacing}}"
        class="at-textarea__textarea" value="{{value}}" confirm-type="完成" maxlength="{{actualMaxlength}}"
        placeholder="{{placeholder}}" disabled="{{disabled}}" auto-focus="{{autoFocus}}"
        focus="{{focus}}" show-confirm-bar="{{showConfirmBar}}" selection-start="{{selectionStart}}"
        selection-end="{{selectionEnd}}" fixed="{{fixed}}" bindinput="handleInput"
        bindfocus="handleFocus" bindblur="handleBlur" bindconfirm="handleConfirm"
        bindlinechange="handleLinechange" data-e-handleInput-so="this" data-e-handleFocus-so="this"
        data-e-handleBlur-so="this" data-e-handleConfirm-so="this" data-e-handleLinechange-so="this"></textarea>
        <block wx:if="{{count}}">
            <view class="{{anonymousState__temp5}}">{{value.length}}/{{maxlength}}</view>
        </block>
    </view>
</block>